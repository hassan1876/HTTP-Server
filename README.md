# Multithreaded HTTP Server

### A simple multithreaded HTTP server in C++ using Winsock for TCP/IP networking on Windows.

## Features

- Handles multiple client connections concurrently.

- Parses HTTP requests and sends basic HTML responses.

- Thread-safe client queue for concurrency management.
